import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.moviewatchlist',
  appName: 'Movie Watchlist',
  webDir: 'dist'
};

export default config;
