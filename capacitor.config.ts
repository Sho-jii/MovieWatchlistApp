import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.moviewatchlist',
  appName: 'MovieWatchlistApp',
  webDir: 'dist'
};

export default config;
